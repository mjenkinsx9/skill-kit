8c8
< # generation and judging are agent-driven — see eval/value-add-test.md.
---
> # generation and judging are agent-driven — see reference/value-add-test.md under the resolved Skill Kit root.
20a21,32
> script_dir="$(cd "$(dirname "$0")" && pwd)"
> script_root="$(cd "$script_dir/.." && pwd)"
> if [[ -f "$script_root/reference/value-add-test.md" ]]; then
>     skill_kit_root="$script_root"
> elif [[ -n "${SKILL_KIT_ROOT:-}" && -f "$SKILL_KIT_ROOT/reference/value-add-test.md" ]]; then
>     skill_kit_root="$SKILL_KIT_ROOT"
> elif [[ -n "${CLAUDE_PLUGIN_ROOT:-}" && -f "$CLAUDE_PLUGIN_ROOT/reference/value-add-test.md" ]]; then
>     skill_kit_root="$CLAUDE_PLUGIN_ROOT"
> else
>     skill_kit_root="$script_root"
> fi
> 
118c130
< run_dir="runs/$run_id"
---
> run_dir="${CLAUDE_PROJECT_DIR:-.}/.skill-kit/runs/$run_id"
128c140
< echo "Run the protocol in eval/value-add-test.md. For each seed below, generate"
---
> echo "Run the protocol in $skill_kit_root/reference/value-add-test.md. For each seed below, generate"
